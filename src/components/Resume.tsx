function Resume() {
  return (
    <section id="resume">
      <h2>Resume</h2>

      <p>
        Download my current resume.
      </p>

      <button>
        DOWNLOAD_RESUME
      </button>
    </section>
  );
}

export default Resume;